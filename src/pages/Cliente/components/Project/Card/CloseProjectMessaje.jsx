import Button from '@components/Botones/Button';
import React from 'react';

const CloseProjectMessaje = () => {
	return (
		<>
			<Button variant="secondary" text="Proyecto Cerrado" />
		</>
	);
};

export default CloseProjectMessaje;
